<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>br.com.daniel.java.quarkus.general</groupId>
    <artifactId>quarkus-overview-chapter-general-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <modules>
        <module>common-utils</module>
        <module>btg-pactual-integration</module>
        <module>application</module>
    </modules>

    <properties>
        <!-- Properties Maven -->
        <compiler-plugin.version>3.8.1</compiler-plugin.version>
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <maven.compiler.release>21</maven.compiler.release>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Properties Quarkus Framework -->
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.version>3.33.3</quarkus.platform.version>

        <!-- Properties Libs in Project -->
        <lombok.version>1.18.34</lombok.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <google.guava.version>33.4.0-jre</google.guava.version>
        <assertj-core-version>3.27.7</assertj-core-version>
        <commons.beanutils.version>1.9.4</commons.beanutils.version>
        <github.javafaker.version>1.0.2</github.javafaker.version>
        <apache.commons.collections.version>4.5.0-M2</apache.commons.collections.version>
        <yaml.snakeyaml.version>2.2</yaml.snakeyaml.version>
        <apache.commons.lang.version>3.18.0</apache.commons.lang.version>
        <apache.commons.csv.version>1.10.0</apache.commons.csv.version>
        <open.csv.version>5.8</open.csv.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
